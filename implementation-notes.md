# Implementation notes

Kept by the agent, reviewed by you. One entry per working block.

## Decisions

- **2026-07-08 — prd.html drafted (v0.1).** Architecture: once-daily batch pipeline on GitHub Actions (00:20 UTC), state committed to the repo as `data/state.json`, deterministic change gate before any model call (per sitrep.yml). USGS fetched via FDSN `updatedafter` cursor (not rolling-window summary feeds) so revisions and delayed runs are handled; USGS identity via `ids` alias sets; GDACS episodes stored as versions; ReliefWeb starts on the no-approval RSS feed with the API v2 client as a drop-in swap once the appname is approved. Triage is impact-based (GDACS Orange+, PAGER yellow+, any new ReliefWeb disaster record); droughts exempt from daily "new" logic. Open decisions listed in prd.html §11.

## Open questions

- Geographic scope: global vs. region-weighted triage thresholds (PRD §11, defaulting to global).
- Quiet-day dashboard behaviour: leave untouched vs. stamp "no significant change" (defaulting to untouched).
- ReliefWeb appname approval pending — submitted when? (Confirm the form actually went in; RSS fallback works meanwhile.)

- **2026-07-08 — Slice 1 built and verified end-to-end (USGS only).** `hadr/` package (fetcher, normalize, store, triage) + `scripts/run_pipeline.py`, `check_changes.py`, `mark_reported.py`. Live run: 313 M4.5+ events fetched via FDSN `updatedafter` cursor, 6 met the PAGER yellow+ threshold (incl. the Venezuela red doublet), gate returned `changed=true`; sitrep hand-written to `dashboard.html` from `facts.json`; after `mark_reported.py`, a second full run returned `changed=false`. Ledger updates are a separate script so a failed sitrep re-surfaces incidents next run instead of losing them. Stdlib only, Python 3.9 compatible.

- **2026-07-08 — Slice 2 built and verified end-to-end (GDACS + correlation).** New `hadr/fetchers/gdacs.py` (EVENTS4APP + lazy Orange+-only detail fetch), `hadr/correlate.py` (GLIDE → USGS-id bridge → fuzzy passes over union-find; correlation links records, never merges identities), GDACS normalization with unified `alert_level`/`revision_signature` fields, per-source triage thresholds (USGS PAGER yellow+, GDACS orange+), drought exemption. State migrated v1→v2 in place (ledger preserved). Live verification: 100 GDACS events ingested; 3 significant (Red typhoon BAVI-26, Orange China flood, Orange France wildfires); 11 GDACS↔USGS earthquake incidents correlated; after publish + mark_reported, full re-run quiet.
  - **Bug found and fixed during verification:** triage originally judged only this-run changes, so a significant incident that fired while no sitrep was published would be silently lost on the next run. Triage now scans every tracked incident against the reporting ledger each run — unpublished incidents re-surface until marked, which is what makes the separate `mark_reported.py` step actually safe.
  - **Live API finding:** GDACS `sourceid` (the USGS event id for NEIC earthquakes) is present as a key but *empty* in EVENTS4APP list payloads — it is only populated in per-event detail. The id bridge therefore rides on the lazy Orange+ detail fetch (FR-2); green earthquakes correlate via the fuzzy pass, verified working on all 11 current pairs.

- **2026-07-08 — Slice 3 built and verified (ReliefWeb RSS, /sitrep skill, 08:30 cron).** New `hadr/fetchers/reliefweb_rss.py` (no appname needed; honest UA passes Cloudflare — verified live; API v2 client swaps in behind the same interface when the appname is approved). ReliefWeb records normalized with no alert level (existence is the signal) and no coordinates, so correlation gained a fourth pass: country + hazard + ≤96 h (day-precision pubDates + editorial lag). `skills/sitrep/SKILL.md` written; `sitrep.yml` enabled (cron 00:20 UTC = 08:20 SGT, claude-code-action with `/sitrep`, deterministic gate and mark_reported around the model step, state committed on quiet runs as proof of life). Live verification: 20 RW records ingested; `editorial_confirmation` fired for `EQ-2026-000093-VEN`, whose GLIDE/country match **bridged the two red Venezuela quakes into one incident** (geo-fuzzy is cross-source-only, so they had been separate units) plus 4 aftershocks; quiet after publish + mark.
  - **Triage decision:** "any new ReliefWeb disaster record" (PRD §6) refined — a record older than 7 days only fires as `editorial_confirmation` when it attaches to an already-reported incident; otherwise the first run would dump ~20 stale disasters into the report. The month-old Philippines record (59-quake aftershock sequence umbrella) correctly stayed quiet under this rule.
  - **mark_reported note:** marking all incident members means sub-threshold aftershocks enter the ledger at their current (null/green) level — a later PAGER upgrade still fires as escalation because rank comparison is against the recorded level.

- **2026-07-08 — Slice 4 built and verified (deletions, retractions, revision tests).** `usgs.fetch_event_status` re-checks previously *reported* USGS events with `includedeleted=true`, tried against every alias id; only positive evidence ("status: deleted") retracts — 404s/network trouble are `unknown` and never trigger. Retraction outranks all other triage reasons, fires once (ledger `retraction_reported` flag, set by mark_reported), and deleted events stop contributing to incident ranks. New `tests/test_revisions.py` (11 tests, offline, stdlib unittest) covers the PRD demo — PAGER yellow→red produces an escalation lead — plus alias-identity merging, downgrade significance, unpublished-incident resurfacing, corroboration non-realerts, the full retraction lifecycle, and both triage policy exemptions. CLAUDE.md test command filled in. Live run: deletion checks found all reported events intact; three-source run quiet.

- **2026-07-08 — Slice 5 built and verified (hardening: retries, staleness drill, snapshots/replay).** Fetchers retry once with a 5 s gap (polite, not a hammer); feed URLs are env-overridable (`HADR_*_URL`) for drills and tests. Pipeline writes raw payload snapshots to `data/snapshots/<stamp>/` (pruned to 14 locally, uploaded as a 14-day CI artifact); `run_pipeline.py --replay <dir>` re-runs fully offline (no fetches, no deletion checks, no enrichment). `sitrep.yml` now runs the offline test suite before the pipeline. Six new tests (retry, staleness accounting, snapshot roundtrip/prune, env overrides) — 17 total, all passing. Live drills: GDACS blocked via env override → fetch failed after retry, run continued, staleness transition woke the gate (`changed=true`) while USGS/ReliefWeb were untouched; recovery run quiet; replay from the day's snapshot reproduced the run with zero network.

- **2026-07-09 — Telegram channel added (ask + push).** `scripts/telegram_bot.py`: local long-polling bot (stdlib only, token in gitignored `.env`, locked to one chat id) answering /status with the latest *published* facts read from `data/facts.json` on main — it never touches feeds, freshness is stated in the first line. `scripts/notify_telegram.py` + a guarded sitrep.yml step: on `changed=true` mornings, a deterministic headline + dashboard link is pushed (never the report body — dashboard.html stays the single traceable artifact); skips silently until the `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID` repository secrets exist, and a failed send warns without failing the morning. Verified: skip path, composed message, /status preview against live facts.

- **2026-07-09 — Telegram bot answers natural-language questions.** Free-form messages now route to Claude via the locally installed Claude Code CLI in headless mode (`claude -p`, prompt over stdin to dodge Windows argv limits) — chosen over the Anthropic SDK because the repo is stdlib-only and the user's Claude access is the Claude Code login, not an API key. The prompt grounds Claude in the published `facts.json` with explicit rules: disaster/monitor claims only from the facts, admit gaps, never invent events; general-knowledge questions answered normally (with normal LLM caveats — these are not monitor data). `/status`, `/dashboard`, `/help` stay deterministic and free. Every failure path (CLI missing, timeout, empty answer) degrades to the deterministic status reply. New `--ask "..."` test mode. Verified live: Japan question answered correctly from facts (BAVI-26 + Noda M6.9); Chile-tsunami question correctly reported as not in the data. Requires the bot to run on a machine with Claude Code installed and logged in; each question spends Claude usage.

## Deviations

- **FR-14 snapshot storage.** The PRD's acceptance ("yesterday's run reproducible from snapshots without network") is met, but snapshots are *not committed* — daily multi-hundred-KB feed dumps would bloat the repo without bound. They live in gitignored `data/snapshots/` locally (pruned to 14 runs) and as GitHub Actions artifacts (14-day retention) in CI. To replay a CI day locally: download the run's artifact, then `python scripts/run_pipeline.py --replay <dir>`.

- **Gate signalling (PRD §5 diagram said "exit quiet / exit changed").** `check_changes.py` always exits 0 on a successful decision and emits `changed=true|false` on stdout and into `$GITHUB_OUTPUT`; exit 2 only when facts.json is missing. Reason: a nonzero exit marks the Actions step failed, which would conflate "changes found" with "pipeline broken" — output-variable branching keeps NFR-3 (failures must be loud, and only failures) intact. PRD FR-11 acceptance is otherwise unchanged.

<!-- Anything built that departs from the PRD or CLAUDE.md is recorded here,
     with the reason. An undocumented deviation is a bug. -->
